@model IEnumerable<_2013105920_ENT.Entities.RanuraDeposito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Atm.Mensaje)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Atm.Mensaje)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RanuraDepositoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RanuraDepositoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RanuraDepositoId })
        </td>
    </tr>
}

</table>
